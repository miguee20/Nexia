/**
 * @nexia/shared-types
 * Central export point for all shared TypeScript types, DTOs and enums.
 * This package is consumed by both @nexia/api and @nexia/web.
 *
 * Exports will be added progressively as each feature module is developed.
 */

// ─── Placeholder export to keep the package valid ───────────────────────────
// Will be replaced with real exports starting in Phase 1.
export const NEXIA_VERSION = '0.1.0';
